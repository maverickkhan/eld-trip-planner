# Base URL of the Django API (no trailing slash).
# Leave empty in development: Vite proxies /api to http://localhost:8000.
# On Vercel set this to the hosted backend, e.g. https://eld-trip-planner-api.onrender.com
VITE_API_BASE_URL=
